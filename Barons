import React from 'react';
import { Text, View } from 'react-native';

const HelloWorldApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}>
      <Text>Hello, world! Group.3 Karlis Barons</Text>
      <Text>This is Group.3 members Karlis Barons first react project</Text>
    </View>
  )
}
export default HelloWorldApp;
